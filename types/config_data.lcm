package forseti2;

struct config_data {
  string ConfigFile;
  boolean IsBlueAlliance;
  int32_t TeamNumber;
  string TeamName;

  // Might be deprecated in favor of defining an LCM format.
  string FieldObjects;
}
