package forseti2;

struct dispenser_cmd
{
	const int32_t TEAM_BLUE = 0;
	const int32_t TEAM_GOLD = 1;

	// State of the dispenser encoded as a byte.
	const int32_t STATE_HELD = 2;
	const int32_t STATE_RELEASED = 3;

	header header;

	/* State of each dispenser*/
	int32_t state[4];

	int32_t team;
}

