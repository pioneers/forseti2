package forseti2;

struct RobotControl {
    // whether or not a robot has been estopped
    boolean estop;

    // student code running on robots
    boolean running;
    
    // robot set to autonomous/teleop mode  (student code can access joysticks)
    boolean autonomous;

    // robot enabled (student code can write to motors)
    boolean enabled;
}
